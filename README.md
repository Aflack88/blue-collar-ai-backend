# Blue Collar AI Backend

Backend API for Blue Collar AI parts search application.

## API Endpoints

- `GET /api/search?q=QUERY` - Search for parts
- `GET /api/health` - Health check
- `GET /` - API documentation

## Example Usage
